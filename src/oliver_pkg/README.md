# Oliver PKG

This package has a publisher which publishes `OliverDouble`s (from `onboarding_msgs`) with one double
doubling each loop and one double halving each loop. Both of their values are sent to `onboarding_echo`
with a short message about them diverging.