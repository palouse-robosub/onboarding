# james_pkg

This package consists of a publisher that publishes 200 degrees Celsius to the "temperature" topic and a subscriber that subscribes to the "temperature" topic and sends a request to the server echoing that temperature, I think.